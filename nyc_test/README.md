## test coverage with nyc in node.js project

```
nyc --reporter=html node [--harmony] sub_spec.js
```

* It will create a folder `coverage/`
* open `coverage/index.html` with Chrome or others
* show a report